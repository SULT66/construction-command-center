--- a/deploy/full-web/app.js
+++ b/deploy/full-web/app.js
@@ -13,7 +13,7 @@
 function statusClass(s){return `status s-${String(s||'draft').toLowerCase()}`}
 function inferScreen(v){if(!v)return'dashboard';return ({draft:'plan',briefing:'briefing',action_required:'ack',approved:'ack',active:'active',paused:'changed',completed:'completed',cancelled:'completed'})[v.status]||'dashboard'}
 function headers(extra={}){return {'Content-Type':'application/json',Authorization:`Bearer ${state.token}`,...extra}}
-async function req(method,path,body,extra={}){if(!state.token){state.authOpen=true;render();throw new Error('AUTH_REQUIRED')}state.busy=true;state.error='';render();try{const r=await fetch(path,{method,headers:headers(extra),body:body===undefined?undefined:JSON.stringify(body)});let data=null;try{data=await r.json()}catch{}if(!r.ok){const code=data?.error||data?.code||`HTTP_${r.status}`;if(r.status===401)state.authOpen=true;if(code==='VERSION_CONFLICT'){await loadView(false).catch(()=>{});throw new Error('Plan changed on another device. Reloaded latest version.')}throw new Error(data?.message||code)}return data}finally{state.busy=false;render()}}
+async function req(method,path,body,extra={}){if(!state.token){state.authOpen=true;render();throw new Error('AUTH_REQUIRED')}state.busy=true;state.error='';render();try{const fetchOptions={method,headers:headers(extra),body:body===undefined?undefined:JSON.stringify(body),cache:'no-store'};if(method==='GET')fetchOptions.headers={...fetchOptions.headers,'Cache-Control':'no-cache','Pragma':'no-cache'};const r=await fetch(path,fetchOptions);let data=null;try{data=await r.json()}catch{}if(!r.ok){const code=data?.error||data?.code||`HTTP_${r.status}`;if(r.status===401)state.authOpen=true;if(code==='VERSION_CONFLICT'){await loadView(false).catch(()=>{});throw new Error('Plan changed on another device. Reloaded latest version.')}throw new Error(data?.message||code)}return data}finally{state.busy=false;render()}}
 async function health(){try{const r=await fetch('/health/ready');return r.ok}catch{return false}}
 async function loadPlans(){if(!state.token)return;const d=await req('GET',`${API}/projects/${encodeURIComponent(state.projectId)}/plans?limit=50`);state.plans=d.plans||[]}
 async function loadView(changeScreen=true){if(!state.planId){state.view=null;return}const v=await req('GET',`${API}/plans/${encodeURIComponent(state.planId)}/view`);state.view=v;localStorage.setItem('ss_plan',v.id);if(changeScreen)state.screen=inferScreen(v)}
@@ -42,13 +42,13 @@
 function crewList(v){return `<div class="list">${v.crew.map(w=>`<div class="row"><div><strong>${esc(w.roleOnTask||'Worker')}</strong><small class="code">${esc(w.workerId)}</small></div><div><span class="${w.eligible?'ok':'bad'}">${w.eligible?'✓ Eligible':'✕ Ineligible'}</span>${w.acknowledged?'<br><span class="ok">✓ Acknowledged</span>':''}</div></div>`).join('')||'<div class="muted">No crew assigned</div>'}</div>`}
 function hazards(v,allowVerify){return `<div class="list">${v.hazards.map(h=>`<div class="card hazard" data-sev="${esc(h.severity)}" style="margin:0"><div class="row" style="border:0;padding:0"><div><strong>${esc(h.hazardType)}</strong><small>${esc(h.description)} · ${esc(h.source)}</small></div><b class="risk-${esc(h.severity)}">${esc(h.severity).toUpperCase()}</b></div>${h.controls.map(c=>`<div class="control"><div class="row" style="border:0;padding:0"><div><b>${esc(c.description)}</b><small>${c.verificationRequired?'Verification required':'Standard control'}</small></div><div>${c.verificationStatus==='verified'?'<span class="ok check">✓ VERIFIED</span>':allowVerify&&c.verificationRequired?`<button class="btn secondary" onclick="window.SS.verifyControl('${c.id}')">Verify</button>`:`<span>${esc(c.verificationStatus)}</span>`}</div></div></div>`).join('')||'<div class="banner warn">No controls — G2 blocked</div>'}</div>`).join('')||'<div class="muted">No hazards identified</div>'}</div>`}
 function briefing(){const v=state.view;if(!v)return noPlan();return `${planHeader('Conduct Briefing','Discuss every hazard, control and Stop Work Authority with the crew')}${v.status==='draft'?`<div class="banner warn">Briefing has not started. <button class="btn primary" onclick="window.SS.startBriefing()">Start briefing</button></div>`:''}<div class="grid2"><div class="card"><h3>Today’s work</h3><b>${esc(v.taskTitle)}</b><p>${esc(v.taskDescription||'')}</p><div class="code">Zone: ${esc(v.zoneId)} · Crew: ${v.crew.length}</div></div><div class="card"><h3>Briefing status</h3>${v.briefings.map(b=>`<div class="row"><div><strong>${b.completedAt?'Completed':'In progress'}</strong><small>${fmt(b.startedAt)}${b.completedAt?' → '+fmt(b.completedAt):''}</small></div><span>${b.conditionsConfirmed?'Conditions confirmed':'Pending'}</span></div>`).join('')||'<div class="muted">No briefing session</div>'}</div></div><div class="card"><h3>Hazards & controls</h3>${hazards(v,true)}</div><div class="banner"><b>Stop Work Authority</b><br>Every worker may stop work if conditions are unsafe or different from this briefing.</div><div class="actions"><button class="btn danger" onclick="window.SS.pause('Worker or supervisor raised safety concern')">Pause / Flag concern</button><button class="btn primary" ${v.status==='briefing'?'':'disabled'} onclick="window.SS.completeBriefing()">Complete briefing →</button></div>`}
-function acknowledgement(){const v=state.view;if(!v)return noPlan();const req=v.crew.filter(x=>x.requiredAck),pending=req.filter(x=>!x.acknowledged);return `${planHeader('Worker Acknowledgement','Every required worker acknowledges this exact SafeStart version')}${summary(v)}<div class="card"><div class="page-title"><div><h3>${req.length-pending.length}/${req.length} acknowledged</h3><div class="muted">Current version: ${v.currentVersion}</div></div></div>${crewAck(v)}</div>${pending.length?`<div class="banner warn"><b>G5 blocked:</b> ${pending.length} worker acknowledgement(s) pending.</div>`:`<div class="banner success"><b>G5 satisfied:</b> all required workers acknowledged.</div>`}<div class="actions">${v.status==='action_required'?`<button class="btn primary" onclick="window.SS.approve()">Approve SafeStart</button>`:''}${v.status==='approved'?`<button class="btn success" onclick="window.SS.activate()">Activate work →</button>`:''}</div>`}
+function acknowledgement(){const v=state.view;if(!v)return noPlan();const req=v.crew.filter(x=>x.requiredAck),pending=req.filter(x=>!x.acknowledged);return `${planHeader('Worker Acknowledgement','Every required worker acknowledges this exact SafeStart version')}${summary(v)}<div class="card"><div class="page-title"><div><h3>${req.length-pending.length}/${req.length} acknowledged</h3><div class="muted">Current version: ${v.currentVersion}</div></div></div>${crewAck(v)}</div>${pending.length?`<div class="banner warn"><b>G5 blocked:</b> ${pending.length} worker acknowledgement(s) pending.</div>`:`<div class="banner success"><b>G5 satisfied:</b> all required workers acknowledged.</div>`}<div class="actions">${v.status==='action_required'?`<button class="btn primary" ${pending.length?'disabled title="Complete all required worker acknowledgements first"':''} onclick="window.SS.approve()">Approve SafeStart</button>`:''}${v.status==='approved'?`<button class="btn success" onclick="window.SS.activate()">Activate work →</button>`:''}</div>`}
 function crewAck(v){return `<div class="list">${v.crew.map(w=>`<div class="row"><div><strong>${esc(w.roleOnTask||'Worker')}</strong><small class="code">${esc(w.workerId)}</small></div>${w.acknowledged?`<span class="ok">✓ ${esc(w.acknowledgement?.method||'ack')}</span>`:`<div class="actions"><button class="btn secondary" onclick="window.SS.ack('${w.workerId}','tag')">Guardian TAG</button><button class="btn secondary" onclick="window.SS.ack('${w.workerId}','signature')">Signature</button></div>`}</div>`).join('')}</div>`}
 function conditions(v){const dyn=v.dynamicConditions||[],evt=v.conditionEvents||[];return `<div class="list">${dyn.map(c=>`<div class="row"><div><strong>${esc(c.metric)} · ${esc(c.action)}</strong><small>${esc(c.status)} · ${fmt(c.lastObservedAt)}</small></div><b class="${c.status==='RESOLVED'?'ok':'bad'}">${esc(c.lastValue)} ${esc(c.unit||'')}</b></div>`).join('')}${evt.slice(0,10).map(c=>`<div class="row"><div><strong>${esc(c.category)}</strong><small>${esc(c.description)} · ${fmt(c.observedAt)}</small></div><b>${esc(c.severity)}</b></div>`).join('')||'<div class="muted">No condition events linked to this plan.</div>'}</div>`}
 function active(){const v=state.view;if(!v)return noPlan();return `${v.status!=='active'?`<div class="banner warn">Plan is ${esc(v.status)}, not ACTIVE.</div>`:''}<div class="hero-active"><div><span class="live">LIVE SAFE START</span><h1>${esc(v.taskTitle)}</h1><p>Version ${v.currentVersion} · Zone ${esc(v.zoneId)}</p></div><button class="btn danger" onclick="window.SS.pause('Manual Stop Work from SafeStart UI')">■ PAUSE WORK</button></div>${summary(v)}<div class="grid2"><div class="card"><h3>Live conditions</h3>${conditions(v)}</div><div class="card"><h3>Crew readiness</h3>${crewList(v)}</div></div><div class="card"><h3>Critical controls</h3>${hazards(v,false)}</div><div class="actions"><button class="btn success" ${v.status==='active'?'':'disabled'} onclick="window.SS.completeWork()">Mark work completed</button></div>`}
 function changed(){const v=state.view;if(!v)return noPlan();return `<div class="hero-paused"><span class="eyebrow" style="color:#ffd8d8">CHANGED CONDITION</span><h1>Work is paused</h1><p>The approved baseline is no longer valid. Direct PAUSED → ACTIVE is blocked.</p></div><div class="grid2"><div class="card"><h3>Condition events</h3>${conditions(v)}</div><div class="card"><h3>Required response</h3><div class="list"><div class="row"><b>1</b><span>Pause work</span><span class="ok">Complete</span></div><div class="row"><b>2</b><span>Reassess hazard / controls</span><span>Required</span></div><div class="row"><b>3</b><span>Create SafeStart v${v.currentVersion+1}</span><span>Required</span></div><div class="row"><b>4</b><span>Re-brief + re-acknowledge</span><span>Required</span></div></div></div></div><div class="banner error"><b>No automatic resume.</b> Sensor normalization does not reactivate work.</div><div class="actions"><button class="btn primary" ${v.status==='paused'?'':'disabled'} onclick="window.SS.createVersion()">Create v${v.currentVersion+1} & re-brief →</button></div>`}
 function completed(){const v=state.view;if(!v)return noPlan();return `${planHeader('Completed SafeStart','Audit-ready project record')}${summary(v)}<div class="grid2"><div class="card"><h3>Plan record</h3><table class="table"><tr><th>Plan</th><td>${esc(v.planNumber)}</td></tr><tr><th>Version</th><td>${v.currentVersion}</td></tr><tr><th>Status</th><td>${esc(v.status)}</td></tr><tr><th>Task</th><td>${esc(v.taskTitle)}</td></tr><tr><th>Zone</th><td class="code">${esc(v.zoneId)}</td></tr><tr><th>Completed</th><td>${fmt(v.completedAt)}</td></tr></table></div><div class="card"><h3>Compliance checks</h3><div class="list"><div>✓ Work scope documented</div><div>✓ Hazards & controls versioned</div><div>✓ Crew eligibility captured</div><div>✓ Worker acknowledgements captured</div><div>✓ Changed conditions preserved</div><div>✓ Immutable audit events preserved</div></div><div class="actions"><button class="btn secondary" onclick="window.print()">Print / Save pilot copy</button></div><small class="muted">Official immutable P5 compliance PDF remains a backend compliance artifact.</small></div></div><div class="card"><h3>Immutable audit trail</h3><div class="timeline">${v.audit.map(a=>`<div class="event"><time>${fmt(a.occurredAt)}</time><div><strong>${esc(a.eventType)}</strong><p>${esc(JSON.stringify(a.payload||{}))}</p><small>${esc(a.actorType)} · ${esc(a.actorId||'system')}</small></div></div>`).join('')||'<div class="muted">No audit events</div>'}</div></div>`}
 function render(){let body;if(state.screen==='new')body=newPlanForm();else if(state.screen==='dashboard')body=dashboard();else if(state.screen==='plan')body=planSetup();else if(state.screen==='briefing')body=briefing();else if(state.screen==='ack')body=acknowledgement();else if(state.screen==='active')body=active();else if(state.screen==='changed')body=changed();else body=completed();$app.innerHTML=shell(body)}
-window.SS={nav,render,choosePlan,refresh:()=>refresh().catch(e=>{state.error=e.message;render()}),openAuth:()=>{state.authOpen=true;render()},closeAuth:()=>{state.authOpen=false;render()},setToken,logout,setProject:v=>{state.projectId=v.trim();localStorage.setItem('ss_project',state.projectId)},newPlan:()=>{state.screen='new';render()},createPlan,addWorker,addHazard,addControl,verifyControl,quickPpe,startBriefing:()=>command('start-briefing').catch(e=>{state.error=e.message;render()}),completeBriefing:()=>command('complete-briefing',{conditionsConfirmed:true}).then(()=>{state.screen='ack';render()}).catch(e=>{state.error=e.message;render()}),ack:acknowledge,approve:()=>command('approve').catch(e=>{state.error=e.message;render()}),activate:()=>command('activate').catch(e=>{state.error=e.message;render()}),pause:r=>command('pause',{reason:r}).catch(e=>{state.error=e.message;render()}),createVersion:()=>command('create-version',{changeSummary:'Changed site condition requires reassessment'}).then(()=>{state.screen='briefing';render()}).catch(e=>{state.error=e.message;render()}),completeWork:()=>command('complete',{reason:'Work scope completed'}).catch(e=>{state.error=e.message;render()})};
+window.SS={nav,render,choosePlan,refresh:()=>refresh().catch(e=>{state.error=e.message;render()}),openAuth:()=>{state.authOpen=true;render()},closeAuth:()=>{state.authOpen=false;render()},setToken,logout,setProject:v=>{state.projectId=v.trim();localStorage.setItem('ss_project',state.projectId)},newPlan:()=>{state.screen='new';render()},createPlan,addWorker,addHazard,addControl,verifyControl,quickPpe,startBriefing:()=>command('start-briefing').catch(e=>{state.error=e.message;render()}),completeBriefing:()=>command('complete-briefing',{conditionsConfirmed:true}).then(()=>{state.screen='ack';render()}).catch(e=>{state.error=e.message;render()}),ack:acknowledge,approve:()=>command('approve').catch(e=>{state.error=e.message;render()}),activate:()=>command('activate').catch(e=>{state.error=e.message;render()}),pause:r=>command('pause',{reason:r}).catch(e=>{state.error=e.message;render()}),createVersion:()=>command('create-version',{changeSummary:'Changed site condition requires reassessment'}).then(()=>{state.screen='briefing';render()}).catch(e=>{state.error=e.message;render()}),completeWork:()=>command('complete',{reason:'Work scope completed'}).then(async()=>{await loadView(false);state.screen=inferScreen(state.view);render();showToast('Work completed')}).catch(e=>{state.error=e.message;render()})};
 render();
 Promise.resolve().then(async()=>{if(state.token){try{await loadPlans();if(state.planId)await loadView(true)}catch(e){state.error=e.message}}else state.authOpen=true;render()});
