--- a/vendor/p3-backend/dist/persistence/postgres/postgres-safestart-api-repository.js
+++ b/vendor/p3-backend/dist/persistence/postgres/postgres-safestart-api-repository.js
@@ -59,7 +59,7 @@
         throw new SafeStartDomainError("NOT_FOUND", "SafeStart plan not found."); if (p.rowVersion !== expected)
         throw new VersionConflictError(expected, p.rowVersion); if (p.status !== "paused")
         throw new SafeStartDomainError("INVALID_TRANSITION", "Re-brief version can only be created from paused state."); const old = await this.getCurrentVersion(planId); if (!old)
-        throw new Error("Current version missing."); const next = p.currentVersion + 1; const v = await this.c.query(`INSERT INTO safe_start_plan_versions(plan_id,version_no,task_title,task_description,zone_id,shift,supervisor_id,subcontractor_id,changed_conditions,change_summary,created_by) VALUES($1,$2,$3,$4,$5,$6,$7,$8,true,$9,$10) RETURNING id`, [planId, next, old.taskTitle, old.taskDescription, old.zoneId, old.shift, old.supervisorId, old.subcontractorId, changeSummary, actorId]); const versionId = v.rows[0].id; await this.c.query(`INSERT INTO safe_start_briefings(plan_version_id,conducted_by,notes) VALUES($1,$2,$3)`, [versionId, actorId, `Re-brief required: ${changeSummary}`]); await this.c.query(`INSERT INTO safe_start_plan_workers(plan_version_id,worker_id,role_on_task,required_ack) SELECT $2,worker_id,role_on_task,required_ack FROM safe_start_plan_workers WHERE plan_version_id=$1 AND removed_at IS NULL`, [old.id, versionId]); await this.c.query(`INSERT INTO safe_start_ppe(plan_version_id,ppe_code,description,required) SELECT $2,ppe_code,description,required FROM safe_start_ppe WHERE plan_version_id=$1`, [old.id, versionId]); await this.c.query(`WITH nh AS (INSERT INTO safe_start_hazards(plan_version_id,hazard_catalog_id,hazard_type,description,severity,source,status,owner_id) SELECT $2,hazard_catalog_id,hazard_type,description,severity,source,status,owner_id FROM safe_start_hazards WHERE plan_version_id=$1 AND removed_at IS NULL RETURNING id,hazard_type,description), oh AS (SELECT id,hazard_type,description FROM safe_start_hazards WHERE plan_version_id=$1 AND removed_at IS NULL) INSERT INTO safe_start_controls(hazard_id,control_catalog_id,description,owner_id,verification_required,verification_status) SELECT nh.id,c.control_catalog_id,c.description,c.owner_id,c.verification_required,CASE WHEN c.verification_required THEN 'pending' ELSE 'not_required' END FROM oh JOIN nh USING(hazard_type,description) JOIN safe_start_controls c ON c.hazard_id=oh.id WHERE c.removed_at IS NULL`, [old.id, versionId]); const q = await this.c.query(`UPDATE safe_start_plans SET current_version=$2,status='briefing' WHERE id=$1 RETURNING id,project_id,current_version,status,supervisor_id,zone_id,shift,work_date,row_version`, [planId, next]); return { plan: mapPlan(q.rows[0]), versionId }; }
+        throw new Error("Current version missing."); const next = p.currentVersion + 1; const v = await this.c.query(`INSERT INTO safe_start_plan_versions(plan_id,version_no,task_title,task_description,zone_id,shift,supervisor_id,subcontractor_id,changed_conditions,change_summary,created_by) VALUES($1,$2,$3,$4,$5,$6,$7,$8,true,$9,$10) RETURNING id`, [planId, next, old.taskTitle, old.taskDescription, old.zoneId, old.shift, old.supervisorId, old.subcontractorId, changeSummary, actorId]); const versionId = v.rows[0].id; await this.c.query(`INSERT INTO safe_start_plan_workers(plan_version_id,worker_id,role_on_task,required_ack) SELECT $2,worker_id,role_on_task,required_ack FROM safe_start_plan_workers WHERE plan_version_id=$1 AND removed_at IS NULL`, [old.id, versionId]); await this.c.query(`INSERT INTO safe_start_ppe(plan_version_id,ppe_code,description,required) SELECT $2,ppe_code,description,required FROM safe_start_ppe WHERE plan_version_id=$1`, [old.id, versionId]); await this.c.query(`WITH nh AS (INSERT INTO safe_start_hazards(plan_version_id,hazard_catalog_id,hazard_type,description,severity,source,status,owner_id) SELECT $2,hazard_catalog_id,hazard_type,description,severity,source,status,owner_id FROM safe_start_hazards WHERE plan_version_id=$1 AND removed_at IS NULL RETURNING id,hazard_type,description), oh AS (SELECT id,hazard_type,description FROM safe_start_hazards WHERE plan_version_id=$1 AND removed_at IS NULL) INSERT INTO safe_start_controls(hazard_id,control_catalog_id,description,owner_id,verification_required,verification_status) SELECT nh.id,c.control_catalog_id,c.description,c.owner_id,c.verification_required,CASE WHEN c.verification_required THEN 'pending' ELSE 'not_required' END FROM oh JOIN nh USING(hazard_type,description) JOIN safe_start_controls c ON c.hazard_id=oh.id WHERE c.removed_at IS NULL`, [old.id, versionId]); await this.c.query(`INSERT INTO safe_start_briefings(plan_version_id,conducted_by,notes) VALUES($1,$2,$3)`, [versionId, actorId, `Re-brief required: ${changeSummary}`]); const q = await this.c.query(`UPDATE safe_start_plans SET current_version=$2,status='briefing' WHERE id=$1 RETURNING id,project_id,current_version,status,supervisor_id,zone_id,shift,work_date,row_version`, [planId, next]); return { plan: mapPlan(q.rows[0]), versionId }; }
     async loadGateInput(planId, versionId, actorAuthorized) {
